# AGENTS.md

## 项目概述

PfinderWithAI 是一个面向企业级微服务场景的根因定位 Agent。它接收线上问题描述，结合系统代码、日志和调用关系，沿跨系统调用链逐层分析，最终给出可追溯的根因判断或下一步调查建议。

当前仓库已完成第一版代码骨架和全合成纵向链路，仍处于真实企业能力接入前的架构验证阶段。产品范围以 `docs/Requirements.md` 为准，当前实现状态和可执行命令见 `README.md`，跨设备接力上下文见 `memory/PROJECT_MEMORY.md`。

## 技术栈与命令

- Python 3.12，使用 uv 管理项目和锁文件。
- LangGraph 负责主调查状态图，Pydantic 负责领域与结构化输出校验。
- Typer/Rich 提供 CLI，SQLite 保存本地调查轨迹。
- 安装：`uv sync --dev`
- 格式与静态检查：`uv run ruff check src tests`、`uv run mypy src/pfinder_ai`
- 测试：`uv run pytest tests -q`
- Fake Demo：`uv run pfinder-ai investigate "订单创建失败" --start-system system-a --trace-id trace-synthetic`

新增或修改 Python 注释和 docstring 时统一使用中文；标识符、协议字段和第三方 API 名称保持其规范写法。

## 当前阶段目标

第一阶段只覆盖可控的简单链路，例如 `A -> B -> C/D`：

1. 从问题描述中提取现象、业务标识（订单号、ID 等）和起始系统。
2. 检索当前系统的代码、日志及调用关系，形成有证据支撑的判断。
3. 若根因不在当前系统，识别下一跳系统并继续调查。
4. 输出结构化诊断结果，并记录完整调查过程以便回放和后续复用。

除非需求明确扩展，不要在第一阶段实现通用自治运维、生产变更、故障自动修复或完整企业级调用图平台。

## 核心原则

- **证据优先**：区分事实、推断和未知项。每个结论都应关联代码位置、日志片段、调用关系或工具结果。
- **可追溯**：记录每一步的输入、查询、证据、判断、置信度和下一步，不只保留最终答案。
- **渐进检索**：先缩小系统、时间范围和业务标识，再获取相关上下文；不要把全部生产日志或整个代码库直接放入模型上下文。
- **结构化边界**：系统间传递明确的调查状态和下一跳信息，避免依赖自由文本隐式串联。
- **人可控**：访问生产数据、扩大查询范围或执行有副作用的操作前，必须遵守权限与审批边界。默认只做只读诊断。
- **技术中立**：RLM、Code Graph 和具体 Agent 框架目前都是候选方案。先通过接口和小型验证确定价值，不把探索性选型写成既定事实。

## 建议的诊断数据模型

新增领域模型时，优先围绕以下概念保持命名一致：

- `IncidentInput`：原始问题、现象、业务标识、起始系统、时间范围。
- `InvestigationStep`：目标系统、执行的查询或工具、发现的证据、判断及置信度。
- `Evidence`：来源类型、来源定位、内容摘要、采集时间和必要的脱敏信息。
- `NextHop`：被调用系统或外部依赖、选择原因、用于继续检索的上下文。
- `DiagnosisResult`：根因或当前最佳假设、证据链、未决问题、建议动作和调查轨迹。

字段名称可随语言规范调整，但不要丢失证据来源、置信度和调查轨迹。

## 架构约束

- 将问题解析、检索、推理、调用链遍历、结果持久化和展示层解耦。
- 通过适配器接入代码仓库、日志平台、容器平台及 LLM，核心领域逻辑不得依赖某个供应商 SDK。
- 检索组件应支持范围限制、结果排序和引用来源；纯文本搜索可以作为基线，但不要假定它是最终方案。
- 调用链遍历必须有终止条件，例如最大深度、已访问系统集合、时间或成本预算，以避免循环调用和无限调查。
- LLM 输出进入业务流程前必须经过结构化解析和校验；解析失败时返回可观察的错误，不要静默猜测。
- 对重复执行和部分失败做明确设计，调查步骤应尽可能可重试、可恢复。

## 安全与隐私

- 不提交真实生产日志、访问令牌、连接串、客户数据或内部地址。
- 日志和模型输入默认脱敏，尤其关注账号、手机号、证件号、订单敏感字段和认证信息。
- 示例和测试使用合成数据或彻底匿名化的数据。
- 任何外部模型调用都应明确数据边界，遵循最小必要上下文原则。
- 诊断结果不得把模型推断表述为已确认事实，也不得默认触发生产操作。

## 开发约定

- 在技术栈确定前，不要仅为占位引入大型框架或基础设施。
- 优先提交小而可验证的纵向切片：输入一个合成故障，经过检索和跨系统追踪，产生结构化结果。
- 公共接口、领域模型和关键决策需要简短文档；重要架构决策建议记录在 `docs/decisions/`。
- 新增配置时提供安全的示例文件，真实密钥只从环境变量或密钥管理系统读取。
- 错误信息应包含调查步骤和上下文，但不得泄露敏感数据。
- 保持变更聚焦；不要顺手重构与当前任务无关的代码。

## 测试与验收

每项实现至少验证与其风险相称的内容：

- 问题解析：缺失字段、模糊描述和多种业务标识。
- 结构化输出：合法输出、模型格式错误和字段缺失。
- 调用链遍历：分支、循环、下一跳缺失、外部依赖和最大深度。
- 检索：无结果、多结果、来源引用和范围过滤。
- 安全：敏感信息脱敏、越权数据源和外部调用边界。
- 可追溯性：最终结论能够回溯到每一步查询和证据。

上述命令是当前仓库的真实工具链。新增依赖或改变命令时，应同步更新本文件、`README.md` 和 `uv.lock`。

## Agent 工作流程

处理仓库任务时：

1. 先阅读 `docs/Requirements.md`、本文件、`memory/PROJECT_MEMORY.md` 及任务涉及目录中的说明。
2. 检查当前仓库状态，尊重已有未提交修改。
3. 若需求与技术选型尚不明确，优先产出最小接口、验证方案或 ADR，不擅自扩大范围。
4. 实现后运行已有的格式化、静态检查和测试；若工具尚不存在，明确说明实际完成的验证。
5. 交付时概述行为变化、验证结果、仍存在的假设或风险。

## 文档同步

发生以下变化时同步更新相关文档：

- 产品范围或诊断流程变化：更新 `docs/Requirements.md`。
- 技术栈、目录结构、启动或测试方式变化：更新 `README.md` 和本文件。
- 关键架构取舍变化：新增或更新 `docs/decisions/` 下的 ADR。
- 诊断数据模型或外部集成契约变化：更新对应设计文档和示例。
